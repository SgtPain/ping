# AntiStealthPing

this is a java programm that looks through all your TextChannels and sends the pings you get to a TextChannel you decide

## Warning
this uses a discord selfbot, which is not allowed by the devs. please make sure to use a private server!
## Dependencies
AntiStealthPing comes with all dependencies and only needs Java 8

## Download
[Current v0.1](https://github.com/laundmo/AntiStealthPing/raw/master/AntiStealthPing_current.zip)
## QuickStart

just edit `args.txt` wit hyout token in the first line, the guild(server) id in the second and the channel id in the third line
then execute the AntiStealthPing.jar file


## how to get token and ID's for `args.txt`

**token:**
     1.  follow this tutorial image and paste the Token (without the: `""`) in the first line of args.txt (replace `token_here`) ![enter image description
    here](https://camo.githubusercontent.com/d3d4ad5526143204a98db268d79eadadf0d03a87/687474703a2f2f692e696d6775722e636f6d2f5569416d4f714d2e706e67)
   

	
**guild id and channel id**

 1. open discord settings 	 
 2. go to apperance
 3. scroll down and turn"developer mode" on
 4. right-click on a your servers symbol and then clikc on "copy id" (PLEASE use your own server! you wouldnt want everyone to see all the messages, and then get banned for spam)
 5. paste the ID in the second line of `args.txt` (replace `guildID_here`)
 6. right-click on a text channel in the server you copied before (!) and click on copy ID
 7. paste the ID in the 3rd line of the `args.txt` (replace `channelID_here`)

## IMPORTANT!!

**do not write anything else in `args.txt`**

# =\=\=\=\=GERMAN\=\=\=\=\=

AntiStealthPing ist ein programm das durch alle kanäle guckt und alle @mention's die du krigst in einen privaten server sendet
## Warnung
die benutzt ein selfbot, allerdings sind selfbots von den discord entwicklern verbotn. bitte gehe sicher das der server den du benutzt private is!
## Dependencies
AntiStealthPing kommt mit allen Dependencies. Das einzige was benötigt wird ist Java  8
## Herunterladen
[Aktuell v0.1](https://github.com/laundmo/AntiStealthPing/raw/master/AntiStealthPing_current.zip)
## QuickStart
man muss nur `args.txt` bearbeiten und das discord token in die erste, eine server id in die zweite und eine textchannel id in die dritte zeile kopieren

dann nurnoch die AntiStealthPing.jar datei ausführen

## wie man das Token und die ID's für `args.txt` kriegt

**token:**
folge diesem tutorial bild und füge das token ohne die `""` zeichen in die erste zeile der `args.txt` datei ein (`token_here` ersetzen) ![enter image description
    here](https://camo.githubusercontent.com/d3d4ad5526143204a98db268d79eadadf0d03a87/687474703a2f2f692e696d6775722e636f6d2f5569416d4f714d2e706e67)

**Guild ID und Channel ID**

 1. discord einstellungen öffnen

 2. den reiter "erscheinungsbild" aufrufen

 3. runterscrollen und den "entwicklermodus" einschalten

 4. rechtsclick auf einen privaten server und auf "ID kopieren" drücken
    (BITTE einen privaten server benutzen, da ansonsten die gefahr
    besteht wegen selfbots gebannt zu werden, und es natürlich viele
    nachrichten schickt)

 5. die id in die zweite zeile von `args.txt` einfügen ("guildID_here"
    ersetzen)

 6. rechtsclick auf einen textchannel in dem server und wieder auf "ID
    kopieren"

 7. die id in die dritte zeile von `args.txt` einfügen ("guildID_here"
    ersetzen)

# WICHTIG!!

**keine anderen dinge in `args.txt` schreiben**
